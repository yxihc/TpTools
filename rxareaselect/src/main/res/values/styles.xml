<resources>

    <style name="RxDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置背景不透明-->
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:backgroundDimAmount">0.6</item>
    </style>

    <!--地址选择的动画-->
    <style name="Dialog_ChooseAddress_Animation">
        <item name="android:windowEnterAnimation">@anim/chooseaddress_dialog_enter</item>
        <item name="android:windowExitAnimation">@anim/chooseaddress_dialog_exit</item>
    </style>

    <!--地址选择的动画-->
    <style name="Dialog_Up_Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_up_enter</item>
        <item name="android:windowExitAnimation">@anim/dialog_up_exit</item>
    </style>

</resources>
