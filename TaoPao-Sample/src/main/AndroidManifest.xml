<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.taopao.rxjavaretrofitcutmvp">

    <uses-sdk
        android:minSdkVersion="23"
        android:targetSdkVersion="27" />

    <!-- 文件读取 -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <!-- 访问网络. 某些位置信息需要从网络服务器获取 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 访问WiFi状态. 需要WiFi信息用于网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 修改WiFi状态. 发起WiFi扫描, 需要WiFi信息用于网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 访问网络状态, 检测网络的可用性. 需要网络运营商相关信息用于网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 访问网络的变化, 需要某些信息用于网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE" />
    <!-- 访问手机当前状态, 需要某些信息用于网络定位 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 相机权限 -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- ==================================小米推送====================================== -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.GET_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <permission
        android:name="com.taopao.rxjavaretrofitcutmvp.permission.MIPUSH_RECEIVE"
        android:protectionLevel="signature" /> <!-- 这里com.xiaomi.mipushdemo改成app的包名 -->
    <uses-permission android:name="com.taopao.rxjavaretrofitcutmvp.permission.MIPUSH_RECEIVE" />

    <!-- 这里com.xiaomi.mipushdemo改成app的包名 -->
    <!-- ==================================小米推送====================================== -->
    <application
        android:name=".app.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launchera"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launchera"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--程序入口-->
        <activity
            android:name=".ui.activity.SplashActivity"
            android:theme="@style/ThemeSplash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.activity.net.CutMvpActivity" />
        <activity android:name=".ui.activity.customview.WebViewActivity" />
        <activity android:name=".ui.activity.customview.FrActivity" />
        <activity android:name=".ui.activity.customview.DialogActivity" />
        <activity android:name=".ui.activity.customview.RandomTextActivity" />
        <activity android:name=".ui.activity.design.ImmersiveActivity" />
        <activity android:name=".ui.activity.customview.SwipeBackActivity" />
        <activity android:name=".ui.activity.design.Design1Activity" />
        <activity android:name=".ui.activity.design.Design2Activity" />
        <activity android:name=".ui.activity.design.Design3Activity" />
        <activity android:name=".ui.activity.design.Design4Activity" />
        <activity android:name=".ui.activity.design.Design5Activity" />
        <activity android:name=".ui.activity.design.ZC1Activity" />
        <activity android:name=".ui.activity.design.SearchViewActivity" />
        <activity android:name=".ui.activity.net.DownLoadApkActivity" />

        <service android:name=".service.DownLoadService" />

        <!-- 解决版本升级下载apk 并安装的时候安卓7.0出错 -->
        <!-- android:name是固定写法。 -->
        <!-- android:authorities可自定义，是用来标识该provider的唯一标识，建议结合包名来保证authority的唯一性。 -->
        <!-- android:exported必须设置成 false，否则运行时会报错java.lang.SecurityException: Provider must not be exported 。 -->
        <!-- android:grantUriPermissions用来控制共享文件的访问权限。 -->
        <!-- <meta-data>节点中的android:resource指定了共享文件的路径。此处的file_paths即是该Provider对外提供文件的目录的配置文件，存放在res/xml/下。 -->
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.taopao.rxjavaretrofitcutmvp.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 监听网络变化的广播 -->
        <receiver
            android:name=".receiver.NetStateReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />
            </intent-filter>
        </receiver>

        <activity android:name=".ui.activity.design.NetStateActivity" />
        <activity android:name=".ui.activity.customview.DragItemActivity" />
        <activity android:name=".ui.activity.design.TabLayoutActivity" />
        <activity android:name=".ui.activity.design.BottomBehaviorActivity" />
        <activity android:name=".ui.activity.design.ZhiHuActivity" />
        <activity
            android:name=".ui.activity.customview.GlideActivity"
            android:label="@string/title_activity_glide"
            android:theme="@style/AppTheme" />
        <activity android:name=".ui.activity.customview.recyclerview.RvBaseAdapterActivity" />
        <activity android:name=".ui.activity.customview.recyclerview.Rv1Activity" />
        <activity android:name=".ui.activity.net.DownLoadFileActivity" />
        <activity android:name=".ui.activity.design.ImageTouActivity" />
        <activity android:name=".ui.activity.MainActivity" />
        <activity android:name=".ui.activity.utils.StringUtilsActivity" />
        <activity android:name=".ui.activity.utils.PermissionActivity" />

        <!-- ==================================小米推送====================================== -->
        <service
            android:name="com.xiaomi.push.service.XMPushService"
            android:enabled="true"
            android:process=":pushservice" />
        <service
            android:name="com.xiaomi.push.service.XMJobService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_JOB_SERVICE"
            android:process=":pushservice" /> <!-- 注：此service必须在3.0.1版本以后（包括3.0.1版本）加入 -->
        <service
            android:name="com.xiaomi.mipush.sdk.PushMessageHandler"
            android:enabled="true"
            android:exported="true" />
        <service
            android:name="com.xiaomi.mipush.sdk.MessageHandleService"
            android:enabled="true" /> <!-- 注：此service必须在2.2.5版本以后（包括2.2.5版本）加入 -->
        <receiver
            android:name="com.xiaomi.push.service.receivers.NetworkStatusReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        <receiver
            android:name="com.xiaomi.push.service.receivers.PingReceiver"
            android:exported="false"
            android:process=":pushservice">
            <intent-filter>
                <action android:name="com.xiaomi.push.PING_TIMER" />
            </intent-filter>
        </receiver>

        <!-- 自定义接受推送的receiver -->
        <receiver
            android:name=".receiver.XiaoMiPushReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.xiaomi.mipush.RECEIVE_MESSAGE" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.MESSAGE_ARRIVED" />
            </intent-filter>
            <intent-filter>
                <action android:name="com.xiaomi.mipush.ERROR" />
            </intent-filter>
        </receiver>
        <!-- ==================================小米推送====================================== -->


        <!--仿美团下拉刷新-->
        <activity android:name=".ui.activity.customview.refresh.MeiTuanRefreshActivity" />
        <!--仿京东到家下拉刷新-->
        <activity android:name=".ui.activity.customview.refresh.JDRefreshActivity"></activity>
    </application>

</manifest>